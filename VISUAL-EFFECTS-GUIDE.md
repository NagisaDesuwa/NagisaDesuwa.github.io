# 🎨 网站视觉效果使用指南

本指南说明如何使用和自定义网站的视觉效果。

## 📦 已添加的文件

1. **assets/css/custom.css** - 自定义样式表
2. **assets/js/custom.js** - 自定义 JavaScript 脚本
3. **_layouts/parallax.html** - 带视差效果的布局模板

## 🚀 如何使用

### 方法 1: 在特定页面启用视差效果

编辑任何页面（如 `_pages/about.md`），在 front matter 中指定使用 `parallax` 布局:

```yaml
---
layout: parallax  # 使用视差布局
permalink: /
title: "关于我"
author_profile: true
---
```

### 方法 2: 在所有页面启用（修改默认布局）

编辑 `_layouts/default.html`，在 `<body>` 标签后添加:

```html
<body>
  <!-- 添加这两行 -->
  <div class="parallax-background"></div>
  <div class="particles-background"></div>

  {% include browser-upgrade.html %}
  ...
</body>
```

## 🎨 已包含的视觉效果

### 1. 滚动视差背景
- **效果**: 渐变背景随滚动动态变化
- **类名**: `.parallax-background`
- **自定义**: 修改 `custom.css` 中的 gradient 颜色

```css
.parallax-background {
    background: linear-gradient(135deg, #你的颜色1 0%, #你的颜色2 100%);
}
```

### 2. 浮动粒子效果
- **效果**: 背景中的浮动小粒子
- **类名**: `.particles-background`
- **自定义**: 修改 `custom.js` 中的 `particleCount` 变量

```javascript
const particleCount = 50; // 改成你想要的数量
```

### 3. 卡片悬停效果
- **效果**: 鼠标悬停时卡片上浮和阴影
- **类名**: `.portfolio-item`, `.post-item`
- **自动应用**: 作品集和博客文章

### 4. 平滑滚动
- **效果**: 点击锚点链接时平滑滚动
- **自动启用**: 所有以 `#` 开头的链接

### 5. 自定义滚动条
- **效果**: 渐变色滚动条
- **自动应用**: 整个网站

### 6. 页面加载动画
- **效果**: 内容淡入
- **类名**: `.page-content`

### 7. 滚动显示动画
- **效果**: 元素滚动到可见区域时显示
- **使用方法**: 给元素添加 `reveal-on-scroll` 类

```html
<div class="reveal-on-scroll">
  这段内容会在滚动到时显示动画
</div>
```

## 🔧 自定义和调整

### 修改渐变颜色

编辑 `assets/css/custom.css`:

```css
.parallax-background {
    /* 游戏风格 - 深色神秘 */
    background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);

    /* 或者明亮活泼 */
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);

    /* 或者自然清新 */
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}
```

### 调整悬停效果强度

编辑 `assets/css/custom.css`:

```css
.portfolio-item:hover {
    transform: translateY(-20px) scale(1.05); /* 增加上浮高度和放大比例 */
    box-shadow: 0 30px 60px rgba(0, 0, 0, 0.3); /* 增加阴影 */
}
```

### 禁用某个效果

#### 禁用粒子效果
在 `custom.js` 中注释掉:

```javascript
// createParticles(); // 禁用粒子
```

#### 禁用鼠标跟随
在 `custom.js` 中保持注释状态:

```javascript
// initMouseFollow(); // 已默认禁用
```

### 添加更多视差元素

在页面内容中添加 `data-parallax` 属性:

```html
<div data-parallax="0.5">
  <h2>这个标题会有视差效果</h2>
</div>
```

速度值范围: `0.1` (慢) 到 `1.0` (快)

## 🎯 推荐配色方案

### 游戏设计师专业风格

```css
/* 深邃科技感 */
background: linear-gradient(135deg, #0f2027 0%, #203a43 50%, #2c5364 100%);

/* 电子竞技 */
background: linear-gradient(135deg, #141e30 0%, #243b55 100%);

/* 创意工作室 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 独立游戏 */
background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
```

## 📱 响应式设计

所有效果都已针对移动设备优化:
- 小屏幕上动画速度减慢
- 悬停效果在移动设备上调整
- 粒子数量自动减少（可在代码中自定义）

## ⚡ 性能优化建议

1. **减少粒子数量**: 如果网站较慢，将 `particleCount` 从 50 减少到 20-30
2. **禁用不需要的效果**: 注释掉不使用的功能
3. **使用静态背景**: 如果动画太占资源，可以使用静态渐变

## 🐛 故障排除

### 效果不显示
1. 确保已清除浏览器缓存
2. 检查浏览器控制台是否有错误
3. 确认文件路径正确

### 动画卡顿
1. 减少粒子数量
2. 禁用动态背景颜色变化
3. 使用更简单的渐变

### 样式冲突
1. 检查 `custom.css` 中的 CSS 优先级
2. 使用 `!important` 标记（谨慎使用）
3. 调整样式定义顺序

## 📚 进一步自定义

可以参考以下资源:
- CSS 渐变生成器: https://cssgradient.io/
- 动画库 Animate.css: https://animate.style/
- 粒子库 Particles.js: https://vincentgarreau.com/particles.js/

## 💡 其他创意想法

1. **添加鼠标轨迹特效**
2. **实现打字机效果**
3. **添加背景音乐控制**
4. **创建交互式 Canvas 背景**
5. **添加主题切换器（明暗模式）**

有问题或需要帮助？请随时咨询！
